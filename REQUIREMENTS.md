# Auto Speaker Notes - 要件定義書

## 概要
PowerPointのプレゼンをアドリブで発表しながら、リアルタイムで各スライドに対応するスピーカーノートを自動生成するWebアプリ。

## ユーザーストーリー
1. ユーザーがPPTXファイルをアップロードする
2. ブラウザ上でスライドが表示される
3. ユーザーが「発表開始」を押し、アドリブでプレゼンを話し始める
4. スライドを切り替えると、そのスライドに対応する音声区間が確定する
5. 音声がリアルタイムでWhisperにより文字起こしされる
6. GPTが文字起こしテキストをスライド内容に基づいて整理・要約し、スピーカーノートを生成する
7. 全スライド終了後、スピーカーノート付きPPTXをダウンロードできる

## 技術スタック

| レイヤー | 技術 | 理由 |
|---------|------|------|
| フロントエンド | React + TypeScript | リアルタイムUI、音声キャプチャ |
| バックエンド | Python (FastAPI) | python-pptx、OpenAI SDK、音声処理が容易 |
| 音声認識 | OpenAI Whisper API | 高精度な日本語対応 |
| テキスト整理 | OpenAI GPT-4o | 文字起こしの整理・要約 |
| PPTX操作 | python-pptx | スライド読み取り、ノート書き込み |

## アーキテクチャ

```
┌─────────────────────────────────────┐
│           ブラウザ (React)            │
│                                     │
│  ┌─────────┐  ┌──────────────────┐  │
│  │スライド   │  │ MediaRecorder    │  │
│  │ビューア   │  │ (音声キャプチャ)   │  │
│  └─────────┘  └──────────────────┘  │
│       │              │              │
└───────┼──────────────┼──────────────┘
        │              │
        ▼              ▼
┌─────────────────────────────────────┐
│        FastAPI バックエンド            │
│                                     │
│  ┌──────────┐  ┌─────────────────┐  │
│  │PPTX解析   │  │音声→ノート変換    │  │
│  │(python-   │  │ Whisper → GPT   │  │
│  │ pptx)    │  │                 │  │
│  └──────────┘  └─────────────────┘  │
│                                     │
│  ┌──────────────────────────────┐   │
│  │ PPTX書き出し (ノート追加)      │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

## 画面構成

### メイン画面
- PPTXアップロードエリア（ドラッグ&ドロップ対応）
- スライドビューア（現在のスライドを大きく表示）
- スライドナビゲーション（前へ/次へボタン、スライド一覧サムネイル）
- 録音コントロール（開始/停止ボタン）
- リアルタイム文字起こし表示エリア
- 生成済みスピーカーノート表示エリア（各スライドごと）
- ダウンロードボタン（ノート付きPPTX）

## API設計

### Backend API

| エンドポイント | メソッド | 説明 |
|--------------|---------|------|
| `POST /api/upload` | POST | PPTXアップロード、スライド画像とテキスト抽出 |
| `POST /api/transcribe` | POST | 音声ファイルをWhisperで文字起こし |
| `POST /api/generate-notes` | POST | 文字起こし+スライド内容からスピーカーノート生成 |
| `POST /api/export` | POST | スピーカーノートをPPTXに書き込みダウンロード |

### データフロー（1スライドあたり）

1. **スライド切り替え検知** → フロントエンドが該当区間の音声をBlobとして切り出し
2. **音声送信** → `POST /api/transcribe` に音声Blob送信
3. **文字起こし結果** → Whisper APIで文字起こし
4. **ノート生成** → `POST /api/generate-notes` にスライドテキスト+文字起こし送信
5. **結果返却** → 整理されたスピーカーノートをフロントエンドに返却

## GPTプロンプト設計（ノート生成）

スライドの内容（テキスト）と発話の文字起こしを入力として：
- 「あー」「えーと」などのフィラーを除去
- 論理的な流れに整理
- スライドの内容に沿った構造化
- 箇条書きではなく、自然な話し言葉ベースのノートとして出力
- スライドに書いていない補足情報（発話に含まれるもの）を保持

## 非機能要件

- 音声録音: ブラウザのMediaRecorder APIを使用（WAV/WebM）
- 対応ブラウザ: Chrome, Edge（MediaRecorder対応ブラウザ）
- 対応言語: 日本語（主）、英語
- ファイルサイズ制限: PPTX 50MBまで
- OpenAI APIキー: ユーザーが画面上で入力（サーバーに保存しない）

## ディレクトリ構成（予定）

```
auto-speaker-notes/
├── backend/
│   ├── main.py              # FastAPIエントリポイント
│   ├── routers/
│   │   ├── upload.py         # PPTXアップロード
│   │   ├── transcribe.py     # 音声文字起こし
│   │   ├── generate.py       # ノート生成
│   │   └── export.py         # PPTX書き出し
│   ├── services/
│   │   ├── pptx_service.py   # PPTX解析・書き出し
│   │   ├── whisper_service.py # Whisper API連携
│   │   └── gpt_service.py    # GPT API連携
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── SlideViewer.tsx
│   │   │   ├── AudioRecorder.tsx
│   │   │   ├── TranscriptDisplay.tsx
│   │   │   ├── NotesPanel.tsx
│   │   │   └── FileUpload.tsx
│   │   ├── hooks/
│   │   │   ├── useAudioRecorder.ts
│   │   │   └── useSlideNavigation.ts
│   │   └── services/
│   │       └── api.ts
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## MVP（最小限の動作版）スコープ

Phase 1（MVP）:
1. PPTXアップロード → スライド画像表示
2. スライド切り替え+音声録音
3. スライドごとの音声をWhisperで文字起こし
4. GPTでスピーカーノート生成
5. ノート付きPPTXダウンロード

Phase 2（改善）:
- リアルタイムストリーミング文字起こし（Whisperのリアルタイム版）
- スピーカーノートの手動編集機能
- 複数言語対応の強化
- プレゼン練習モード（既存ノートとの比較）
